# راهنمای Admin واقعی و تست Commerce در مُدارا

## ۱. اجرای migration

در پروژه Supabase، ابتدا فایل زیر را در SQL Editor اجرا کنید:

```text
supabase/migrations/20260822130000_admin_backoffice.sql
```

این migration جدول‌های `coupons`، `customer_profiles` و `store_settings` را ایجاد می‌کند و مجوزهای RLS لازم برای مدیر را فعال می‌سازد.

## ۲. ایجاد کاربر مدیر

در Supabase به بخش **Authentication → Users** بروید و یک کاربر واقعی بسازید. سپس در SQL Editor، مقدار `app_metadata` او را با `auth.users` به نقش مدیر تنظیم کنید. مقدار نمونه زیر را با ایمیل واقعی مدیر جایگزین کنید:

```sql
update auth.users
set raw_app_meta_data = coalesce(raw_app_meta_data, '{}'::jsonb)
  || jsonb_build_object('role', 'admin')
where email = 'admin@example.com';
```

برای اطمینان از تنظیم نقش:

```sql
select id, email, raw_app_meta_data
from auth.users
where email = 'admin@example.com';
```

باید در `raw_app_meta_data` مقدار زیر دیده شود:

```json
{"role":"admin"}
```

کاربر باید از حساب خارج و دوباره وارد شود تا JWT جدید با نقش مدیر دریافت کند. رمز عبور واقعی را هرگز در کد frontend، Git یا فایل‌های عمومی قرار ندهید.

## ۳. تنظیم متغیرهای محیطی

در محیط build پروژه این متغیرها را تنظیم کنید:

```text
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_PUBLIC_ANON_KEY
```

کلید `service_role` نباید در Vite، GitHub Pages یا مرورگر قرار بگیرد. عملیات حساس ادمین باید با RLS و در صورت نیاز با Edge Function امن انجام شود.

## ۴. تست تب کوپن در حالت Demo

به آدرس زیر بروید و با حساب Demo وارد شوید:

```text
https://mcdonun1-hub.github.io/ModernAra1/#backoffice-login
```

نام کاربری `admin` و رمز عبور `admin 1234` را وارد کنید. در تب **تخفیف و کوپن**، کوپن `WELCOME10` را ویرایش کنید، مقدار را موقتاً از ۱۰ به ۱۵ درصد تغییر دهید و ذخیره کنید. کارت کوپن باید مقدار ۱۵٪ و پیام موفقیت ذخیره را نشان دهد. پس از پایان تست، مقدار را به ۱۰٪ برگردانید.

فرم کوپن شامل کد، نوع تخفیف درصدی یا مبلغ ثابت، مقدار، حداقل مبلغ سفارش، سقف مصرف، تاریخ انقضا و وضعیت فعال است.

## ۵. تست ارسال رایگان

در تب **تنظیمات**، مقدار «حداقل مبلغ ارسال رایگان» را موقتاً از ۵۰۰٬۰۰۰ به ۷۵۰٬۰۰۰ تومان تغییر دهید و روی «ذخیره تنظیمات» کلیک کنید. مقدار باید پس از ذخیره در فیلد باقی بماند. سپس مقدار را به ۵۰۰٬۰۰۰ تومان بازگردانید.

در نسخه فعلی، این دو فرم و ذخیره‌سازی پنل تست شده‌اند. اتصال این مقادیر به محاسبه نهایی checkout باید در مرحله پرداخت production به‌صورت server-side نیز اعمال شود؛ صرفاً ذخیره مقدار در پنل به‌تنهایی مبلغ فاکتور را تغییر نمی‌دهد.

## ۶. نمونه‌های لباس

در حالت Demo، `src/lib/demoSeed.ts` همیشه شامل محصولات نمونه دسته `cat-clothing` است. در حالت Production نیز فایل `supabase/migrations/20260820120000_reseed_full_catalog.sql` محصولات لباس را در جدول `products` ثبت می‌کند. در صفحه اصلی، دسته «لباس» به‌صورت نمونه پیش‌فرض باز است و فقط محصولات لباس را نمایش می‌دهد. وقتی کاربر روی دسته دیگری کلیک کند، همان دسته جایگزین می‌شود؛ اگر داده آن دسته در Supabase موجود نباشد، نمونه‌های همان دسته از seed نمایش داده می‌شوند. در صفحه Shop نیز در صورت خالی‌بودن پاسخ کاتالوگ، fallback نمونه لباس فعال می‌شود.

## ۷. محدودیت حالت Demo

داده‌های Demo در `localStorage` همان مرورگر ذخیره می‌شوند و بین کاربران یا دستگاه‌ها مشترک نیستند. برای ذخیره‌سازی واقعی و مشترک، باید credentials واقعی Supabase تنظیم، migration اجرا و نقش Admin برای کاربر تعیین شود.
